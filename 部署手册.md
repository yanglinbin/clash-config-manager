# Clash 配置管理器 · 部署手册

> 面向运维/自建的完整部署与运维指南。技术栈：**Node.js 20 + TypeScript + Fastify**，前端同为 TypeScript。
> 默认以 **Docker Compose** 部署（应用容器 + Nginx 反代容器），也支持裸机直接运行。

---

## 目录

- [1. 系统概览](#1-系统概览)
- [2. 前置要求](#2-前置要求)
- [3. 目录结构](#3-目录结构)
- [4. 部署步骤（Docker Compose）](#4-部署步骤docker-compose)
- [5. 对外访问与 HTTPS](#5-对外访问与-https)
- [6. 环境变量参考](#6-环境变量参考)
- [7. config.ini 配置参考](#7-configini-配置参考)
- [8. 规则更新与 GitHub Webhook](#8-规则更新与-github-webhook)
- [9. 常用运维命令](#9-常用运维命令)
- [10. 裸机运行（不使用 Docker）](#10-裸机运行不使用-docker)
- [11. 排障](#11-排障)
- [12. 安全建议](#12-安全建议)
- [13. 附录](#13-附录)

---

## 1. 系统概览

Clash 配置管理器会读取 `config/config.ini`（订阅源、地区、过滤、代理组）与
`config/rules.yaml`（分流规则），生成一份可用的 Clash/Mihomo 订阅配置，并通过 Web 界面/接口对外提供。

**核心能力**

- 单一供应商模式：`config.ini` 可维护多个订阅源，只启用 `active_provider` 指定的一个
- 订阅链接模式：生成时把订阅链接写入 `proxy-providers`，节点由客户端拉取，服务器无需访问订阅源
- 智能地区分组 + 关键词过滤
- 生成前校验：组名唯一、引用完整、类型合法、规则格式正确；失败不覆盖旧配置
- 更新触发：页面/接口手动触发、GitHub Webhook 推送即时触发（无定时自动更新）
- Web 管理界面 + 状态/更新接口

**架构**

```
客户端 ──HTTP──► Nginx(容器:80) ──► app(容器:5000, Node/Fastify)
                                     ├─ 读取 ./config (只读挂载)
                                     └─ 写出 ./output, ./logs (可写挂载)
```

---

## 2. 前置要求

| 项目 | 要求 |
|------|------|
| Docker | 20.10+ |
| Docker Compose | v2（`docker compose` 子命令） |
| 操作系统 | Linux / Windows(WSL/Docker Desktop) / macOS |
| 网络 | 首次构建需要能访问 npm registry 与 Docker Hub（拉取 `node:20-alpine`、`nginx:1.30-alpine`） |
| 端口 | 对外暴露端口默认 `80`（可用 `HTTP_PORT` 修改） |

> 若要使用 Webhook 自动触发，需要一个可被公网访问的域名/端口（见第 8 节）。

---

## 3. 目录结构

```
clash-config-manager/
├── src/                    # 后端 TypeScript 源码
│   ├── index.ts                   # 服务入口
│   ├── server/ services/ clash/ cli/ utils/
│   └── web/app.ts                 # 前端源码（编译到 public/js）
├── public/                 # 静态资源（由 Fastify 托管）
│   ├── index.html  css/  js/(产物)
├── tests/                  # vitest 测试
├── config/                 # 【运行必需】配置目录（挂载进容器）
│   ├── config.ini          # 主配置（需自行创建）
│   ├── config.ini.example  # 配置示例
│   ├── rules.yaml          # 分流规则
│   └── rules.schema.json
├── clash_rules/            # 规则集文本（Microsoft.txt，被 rules.yaml 引用）
├── output/                 # 生成结果（clash_profile.yaml / stats.json / last_update.txt）
├── logs/                   # 日志（app.log / clash_generator.log）
├── nginx/
│   ├── default.conf        #   Nginx 站点配置（HTTPS，绑定挂载）
│   └── certs/              #   TLS 证书（自行上传；含 README）
├── dist/                   # 服务端构建产物（自动生成）
├── .github/workflows/ci.yml  # CI
├── Dockerfile              # Node 多阶段镜像
├── compose.yaml            # 编排（app + nginx）
├── .env.example            # 环境变量示例
└── package.json tsconfig.json tsconfig.web.json tsup.config.ts vitest.config.ts
```

---

## 4. 部署步骤（Docker Compose）

### 4.1 获取代码

```bash
git clone <你的仓库地址> clash-config-manager
cd clash-config-manager
```

> 也可以直接把整个目录上传到服务器。

### 4.2 准备环境变量 `.env`

```bash
cp .env.example .env
```

按需修改（最常用的是 `HTTP_PORT` 与 `GITHUB_WEBHOOK_SECRET`）：

```dotenv
TZ=Asia/Shanghai
HTTP_PORT=80
LOG_LEVEL=info
GITHUB_WEBHOOK_SECRET=            # 留空则禁用 webhook；启用时填随机密钥
```

生成 Webhook 密钥：

```bash
openssl rand -hex 32
```

### 4.3 准备主配置 `config/config.ini`

```bash
cp config/config.ini.example config/config.ini
vim config/config.ini
```

**至少需要改：**

```ini
[proxy_providers]
# 订阅源名称（大写引用） = 订阅链接
MY_PROVIDER = https://your-subscription-url

[provider_control]
# 指定唯一参与生成的订阅源（必填，大小写不敏感）
active_provider = MY_PROVIDER
```

> `config.ini` 与 `rules.yaml` 都以**只读**方式挂载进容器，修改后重新生成配置即生效。

### 4.4 （可选）创建 Docker 网络

若与其他容器互联（例如使用外部 Nginx）：

```bash
docker network create docker-shared-net
```

### 4.5 构建并启动

```bash
docker compose up -d --build
```

- `app` 服务按根目录 `Dockerfile` **本地构建**（不拉取远程应用镜像）
- `nginx` 服务使用固定版本镜像 `nginx:1.30-alpine`（在 `compose.yaml` 中指定，避免意外拉到旧缓存镜像）
- 首次构建会执行 `npm ci` 与 `npm run build`（tsup 编译服务端 + tsc 编译前端）

### 4.6 验证

```bash
docker compose ps
docker compose logs -f app
```

健康检查（容器内置）会访问 `http://127.0.0.1:5000/status`。

浏览器/接口验证：

```bash
curl -s http://localhost/status
curl -s http://localhost/clash_profile.yaml | head
```

- 管理界面：`http://<服务器IP>/`
- 服务状态：`http://<服务器IP>/status`
- Clash 配置：`http://<服务器IP>/clash_profile.yaml`

> 若首屏显示“配置文件: 不存在”，点击页面“更新配置”，或执行
> `docker compose exec clash-config-manager node dist/cli/generate.js` 立即生成一次。

---

## 5. 对外访问与 HTTPS

### 5.1 使用内置 Nginx + 上传证书（默认）

`compose.yaml` 已内置 nginx，`HTTP_PORT`（默认 80）/ `HTTPS_PORT`（默认 443）直接对外。
站点配置 `nginx/default.conf` 为 **HTTPS**：80 全部 301 跳转到 443，443 做 TLS 终止并反代到 `http://app:5000`。

**证书不自动申请**，由你自行获取后以**文件方式上传**：

| 宿主机 | 容器内（只读） | 内容 |
|--------|----------------|------|
| `nginx/certs/cert.crt` | `/etc/nginx/certs/cert.crt` | 证书（含中间证书链） |
| `nginx/certs/cert.key` | `/etc/nginx/certs/cert.key` | 私钥 |

```bash
# 把证书与私钥放进 nginx/certs/（文件名需与 nginx/default.conf 一致）
cp cert.crt cert.key nginx/certs/

# 确认文件确实到位（宿主机侧）
ls -l nginx/certs/

# 重建 nginx 使其读取证书
cd /opt/clash-config-manager
docker compose up -d --force-recreate nginx
```

**域名**：改 `nginx/default.conf` 里的 `server_name`（默认 `clash.yilabao.top`）；文件名不同时同步改
`ssl_certificate` / `ssl_certificate_key` 路径。改后 `docker compose up -d --force-recreate nginx`。

验证：

```bash
curl -sI http://clash.yilabao.top | head -1      # 期望 301，Location: https://...
curl -sI https://clash.yilabao.top/status | head -3
ss -lntp | grep :443
```

> 私钥已在 `.gitignore` 中忽略（`nginx/certs/*`），**切勿提交**。
> 如不想自己管证书，可改成在容器/宿主机内部用 certbot、acme.sh 等自行签发后再放入本目录。

### 5.2 使用你自己的 Nginx + 域名 + HTTPS

1. 把 app 容器接入你的共享网络（在 `compose.yaml` 的 `app` 下加：
   `networks: [docker-shared-net]`，并在文件底部声明外部网络）。
2. 在你的 Nginx 里反代到 `http://clash-config-manager:5000`，证书按你现有方式配置。

```nginx
server {
    listen 443 ssl;
    server_name clash.example.com;
    # ssl_certificate ...; ssl_certificate_key ...;

    location / {
        proxy_pass http://clash-config-manager:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 180s;
    }
}
```

> **Webhook 必需 HTTPS + 公网域名**（GitHub 会校验 Payload URL）。

---

## 6. 环境变量参考

| 变量 | 作用 | 默认 | 说明 |
|------|------|------|------|
| `HTTP_PORT` | Nginx 对外 HTTP 端口 | `80` | 仅 compose 使用 |
| `HTTPS_PORT` | Nginx 对外 HTTPS 端口 | `443` | 仅 compose 使用；启用 HTTPS 后使用 |
| `IMAGE` | 本地构建产物的镜像名/标签 | `clash-config-manager:latest` | 仅 compose 使用 |
| `TZ` | 时区 | `Asia/Shanghai` | 影响日志与时间显示 |
| `LOG_LEVEL` | 日志级别 | `info` | `DEBUG/INFO/WARNING/ERROR` |
| `APP_PORT` | 应用监听端口（容器内） | `5000` | 与 `nginx/default.conf` 的 `proxy_pass` 保持一致 |
| `GITHUB_WEBHOOK_SECRET` | Webhook 签名校验密钥 | 空（禁用 webhook） | 见第 8 节 |

> **生成行为（更新间隔、启用订阅源、规则路径）不通过环境变量控制**，统一在 `config/config.ini` 中维护。

---

## 7. config.ini 配置参考

```ini
[proxy_providers]
MY_PROVIDER = https://your-subscription-url

[provider_control]
active_provider = MY_PROVIDER        # 必填：唯一启用的订阅源

[regions]
香港 = Hong Kong,HK,香港,HongKong     # 节点名包含任一关键词即归入该地区组
日本 = Japan,JP,日本,Tokyo

[filter]
exclude_keywords = 剩余,官网,到期     # 命中任一关键词的节点不会出现在任何组

[files]
rules_config = config/rules.yaml     # 规则文件路径（只来自本地）

[clash]
port = 7890
socks_port = 7891
allow_lan = true
mode = Rule
log_level = info
external_controller = :9090
test_url = http://connectivitycheck.gstatic.com/generate_204

[merged_regions]
default_type = url-test              # 地区组类型: select/url-test/fallback/load-balance

[relay_groups]                       # 可选：中继组（默认注释关闭）
#name = 统一代理
#type = fallback
#regions = 香港,台湾,日本,美国,新加坡

[proxy_group_defaults]               # 可选：各主组默认选中项
海外代理 = 香港
全球直连 = DIRECT
```

> 组类型优先级：`[merged_regions] 地区键` > `[merged_regions] default_type` >
> `[clash] group_type_地区` > `[clash] default_group_type`。

---

## 8. 规则更新与 GitHub Webhook

### 8.1 规则来源

规则**只来自容器内挂载的本地文件** `config/rules.yaml`（不做远程拉取）。
因此更新流程是：**先把宿主机的 `config/rules.yaml` 更新到最新，再触发重新生成**。

> 另外，`rules.yaml` 里的 `Microsoft` rule-provider 指向本仓库中的
> `clash_rules/Microsoft.txt`（GitHub raw 直链）。若你重命名/移动了该文件，
> 需同步修改 `rules.yaml` 的 URL **并 push 到 GitHub**，否则客户端拉取会 404。

若部署目录是 git 仓库，更新规则：

```bash
cd /path/to/clash-config-manager
git pull            # 更新 config/rules.yaml
docker compose exec clash-config-manager node dist/cli/generate.js
```

如果宿主机没有自动 `git pull`，即使 Webhook 触发，读到的仍是旧规则。建议在宿主机加一个定时任务：

```bash
# 例：每 5 分钟拉取一次（crontab -e）
*/5 * * * * cd /path/to/clash-config-manager && git pull --ff-only >> logs/git-pull.log 2>&1
```

### 8.2 启用 Webhook（推送即触发）

1. 在 `.env` 设置密钥（与第 4.2 节一致）并重启：

   ```bash
   echo "GITHUB_WEBHOOK_SECRET=$(openssl rand -hex 32)" >> .env
   docker compose up -d
   ```

2. GitHub 仓库 → **Settings → Webhooks → Add webhook**：

   | 字段 | 值 |
   |------|-----|
   | Payload URL | `https://<你的域名>/webhook/github` |
   | Content type | `application/json` |
   | Secret | 与 `GITHUB_WEBHOOK_SECRET` **完全相同** |
   | Which events | 仅勾选 **Just the push event** |

3. 保存后 GitHub 会发一个 `ping` 事件，返回 `{"status":"pong"}` 即成功。

**行为**

- 仅当 push 涉及 `config/rules.yaml` 时触发；其他变更返回 `202 ignored`
- 接口立即返回 `202 accepted`，生成在后台进行（避免 GitHub 超时）
- 签名不符返回 `401`；未配置密钥返回 `503`
- **前提**：容器按 `GITHUB_WEBHOOK_SECRET` 校验 HMAC，需与 GitHub Secret 一致

### 8.3 手动触发

```bash
# 浏览器/接口
curl -X POST http://localhost/update-config

# 容器内直接生成
docker compose exec clash-config-manager node dist/cli/generate.js
```

---

## 9. 常用运维命令

```bash
# 构建 / 重新构建镜像（代码或依赖变更后）
docker compose build
docker compose up -d --build

# 状态
docker compose ps

# 日志
docker compose logs -f
docker compose logs -f app
docker compose logs -f nginx

# 重启 / 停止 / 删除
docker compose restart
docker compose down
docker compose down -v        # 连同匿名卷

# 手动生成配置
docker compose exec clash-config-manager node dist/cli/generate.js

# 规则文件 lint（重复规则 / 重叠 CIDR / 无效引用）
docker compose exec clash-config-manager node dist/cli/lint.js

# 修改 Nginx 配置后
docker compose restart nginx
```

**查看生成结果**（宿主机）：

- `output/clash_profile.yaml`：生成的 Clash 配置
- `output/stats.json`：最近一次生成的统计
- `output/last_update.txt`：上次更新时间
- `logs/app.log`、`logs/clash_generator.log`

---

## 10. 裸机运行（不使用 Docker）

需要 **Node.js ≥ 20**。

```bash
npm install
npm run build          # tsup 编译服务端 + tsc 编译前端
npm start              # 启动服务（默认 0.0.0.0:5000）
```

自定义端口：

```bash
APP_PORT=8080 npm start
```

常用脚本：

| 命令 | 说明 |
|------|------|
| `npm run dev` | 开发模式（tsx 热跑 `src/index.ts`） |
| `npm run build` | 构建服务端(`dist/`) + 前端(`public/js/`) |
| `npm start` | 运行构建产物 `dist/index.js` |
| `npm run generate` | 手动生成配置 `dist/cli/generate.js` |
| `npm run lint:rules` | 规则文件 lint |
| `npm test` | vitest（含黄金文件对照） |
| `npm run typecheck` | 类型检查 |

> 裸机运行时，`config/`、`output/`、`logs/` 位于项目根目录，需保证进程工作目录为项目根目录。

---

## 11. 排障

| 现象 | 排查 |
|------|------|
| 容器起不来 / 重启循环 | `docker compose logs app`；确认 `config/config.ini` 存在且 `active_provider` 已配置 |
| 页面正常但“配置文件: 不存在” | 点“更新配置”或 `docker compose exec ... node dist/cli/generate.js`，再看 `logs/clash_generator.log` |
| `/status` 返回 500 | 检查 `config/config.ini` 是否语法错误（节名、`=`），查看 `logs/app.log` |
| 端口被占用 | 改 `.env` 的 `HTTP_PORT`，`docker compose up -d` |
| 生成失败“保留现有配置” | 生成前校验未通过；日志会逐条列出问题（组名重复/悬空引用/规则格式） |
| Webhook 返回 401 | Secret 与 GitHub 不一致；确认 `.env` 已生效（`docker compose up -d` 重建容器） |
| Webhook 返回 503 | 未设置 `GITHUB_WEBHOOK_SECRET` |
| Webhook 触发但配置未变 | 宿主机 `config/rules.yaml` 未更新（没 `git pull`），见 8.1 |
| 打开页面样式/脚本 404 | 未构建前端；执行 `npm run build`（容器镜像已内置产物） |
| HTTPS 打不开 / nginx 无法启动 | 证书缺失或路径不符：`docker compose logs nginx`；确认 `nginx/certs/cert.crt` 与 `nginx/certs/cert.key` 存在，且文件名与 `nginx/default.conf` 的 `ssl_certificate` / `ssl_certificate_key` 一致 |
| 80 未自动跳到 443 | `nginx/default.conf` 仍是 HTTP 版；按 5.2 重新生成配置并 `docker compose up -d nginx` |
| 证书续期后仍用旧证书 | 续期脚本会自动 reload；若手动续期请执行 `docker compose exec nginx nginx -s reload` |
| 时区不对 | 设置 `TZ`（默认 `Asia/Shanghai`） |

**查看容器内真实生效的环境变量点：** `docker compose exec clash-config-manager env | sort`

---

## 12. 安全建议

- `config/config.ini` 含订阅链接、`output/clash_profile.yaml` 含节点信息，**不要提交到 Git**（已在 `.gitignore`）
- 生产环境务必用 **HTTPS** 暴露；Webhook 依赖签名保护，勿关闭 `GITHUB_WEBHOOK_SECRET`
- `POST /update-config` 为公开触发接口（无鉴权）。若暴露公网，建议在 Nginx 层限制来源或加 Basic Auth：
  ```nginx
  location = /update-config {
      allow 127.0.0.1;
      deny all;
      proxy_pass http://app:5000;
  }
  ```
- 定期轮换 `GITHUB_WEBHOOK_SECRET`
- 遵循最小权限：`config` 以只读挂载，仅 `output`/`logs` 可写

---

## 13. 附录

### 13.1 HTTP 接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/` | GET | Web 管理界面 |
| `/status` | GET | 服务状态（JSON） |
| `/update-config` | POST | 触发配置更新 |
| `/webhook/github` | POST | GitHub Webhook（HMAC-SHA256 校验） |
| `/clash_profile.yaml` | GET | 获取生成的 Clash 配置 |
| `/static/*` | GET | 静态资源（css/js） |

### 13.2 端口

| 端口 | 用途 |
|------|------|
| `${HTTP_PORT}`（默认 80） | Nginx 对外监听 |
| `5000` | app 容器内部监听（对外不直接暴露） |
| `7890 / 7891 / 9090` | 生成配置里的 Clash 端口（由客户端使用，非本服务） |

### 13.3 数据与持久化

| 路径（宿主机） | 容器路径 | 权限 | 内容 |
|----------------|----------|------|------|
| `./config` | `/app/config` | 只读 | `config.ini`、`rules.yaml` |
| `./output` | `/app/output` | 读写 | 生成的配置、统计、时间戳 |
| `./logs` | `/app/logs` | 读写 | 运行日志 |
| `./nginx/default.conf` | `/etc/nginx/conf.d/default.conf` | 只读 | 反代配置 |
| `./nginx/certs` | `/etc/nginx/certs` | 只读 | TLS 证书（自行上传） |

### 13.4 版本更新

```bash
cd /path/to/clash-config-manager
git pull
docker compose up -d --build     # 重新构建并滚动更新
docker compose ps
```

回滚：

```bash
git checkout <上一个版本 tag/commit>
docker compose up -d --build
```
